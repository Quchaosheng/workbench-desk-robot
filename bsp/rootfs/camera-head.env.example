# Replace only from the purchased unit label or `rs-enumerate-devices` output.
# The service must not start while this placeholder remains.
D435_SERIAL=TBD_FROM_PURCHASED_UNIT
ROS_DISTRO=TBD_COMPATIBILITY_SELECTION
