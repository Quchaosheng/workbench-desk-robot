{
  "issue": "robot-bsp-freeze-v0.1",
  "human_owner": "Quchaosheng",
  "objective": "Define the robot Linux-board and multi-MCU BSP V0.1 logical topology, ownership, interface allocation, IRQ rules, power boundaries, and freeze gates without claiming physical hardware validation.",
  "allowed_paths": [
    "docs/architecture/robot-bsp-v0.1.md",
    "docs/architecture/robot-bsp-manifest-v0.1.yaml",
    "docs/architecture/robot-bsp-selection-v0.1.md",
    "docs/task_packets/robot-bsp-freeze-v0.1.json"
  ],
  "read_only_paths": ["interfaces/**", "firmware/**", "robot/control/**"],
  "forbidden": ["interfaces/**", "firmware/**", "robot/control/**"],
  "acceptance": [
    "the document and manifest freeze one Linux board and six controller domains, including two robot-owned MCUs and four arm/tool module domains",
    "the cost-conscious prototype selection identifies a Linux board, two robot-owned MCU parts, and supplier-controlled arm/tool domains without claiming AVL or safety approval",
    "logical peripherals have owners and IRQ/DMA allocation rules without invented pin numbers",
    "multi-node CAN addressing and unresolved physical decisions are explicit",
    "the document does not claim physical BSP bring-up or production safety approval"
  ],
  "commands": [
    "python -m json.tool docs/task_packets/robot-bsp-freeze-v0.1.json",
    "python -c \"from pathlib import Path; import yaml; d=yaml.safe_load(Path('docs/architecture/robot-bsp-manifest-v0.1.yaml').read_text()); assert d['linux_board']['model'].startswith('NVIDIA_JETSON_ORIN_NANO'); assert len(d['robot_owned_mcus']) == 2\"",
    "python -c \"from pathlib import Path; p=Path('docs/architecture/robot-bsp-v0.1.md'); assert p.is_file() and 'six controller domains per robot' in p.read_text(encoding='utf-8')\""
  ],
  "evidence": ["document review", "JSON parse output"],
  "stop_conditions": ["a physical part number or pin assignment is presented as confirmed without an owner source", "the Linux board is allowed to bypass MCU-SAFETY"]
}
